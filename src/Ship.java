public class Ship {
    // Parameter
    int length;
    Field start;
    String direction;

    // Constructor
    Ship(){}
    Ship(int length, Field start, String direction){
        this.length = length;
        this.start = start;
        this.direction = direction;
    }

    // Methods

}
